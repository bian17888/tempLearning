// 定义变量
const str = "beck";

// 定义对象
const config = {
  name: "lib.js",
  size: "10kb"
};

// 定义函数
const initNum = 0;
const addX = function(value) {
  return initNum + value;
};

export { str, config, addX };
